
// 1. Integer - int ----> whole numbers
int age;
// Above we just created a variable named age and its type is integer aka. we just declared a variable

age = 10;
// Above we just assigned a value to the variable aka. Assignment


// int year = 2026; // initialization - we created a variable and assign a value to it, it is more practical than the above example

int result = 10 + 90 - 20 + 23;

int number1  = 10; // int is a variable, number1 is the name for future reference, 10 is the value
int number2  = 90;
int calcResult = number1 + number2 - 20 + 23;

// 2.  Floating points - Decimal numbers
double discount = 20.05;

// below we are trying to calculate the are of a triangle using Floating point
double tBase = 12.05;
double tHight = 10.05;
double tArea = (tBase + tHight) / 2;

// 3. Character type - A signle character, assigned with '' and can only hold 1 character
char sign = 'A';
char other = '?';
char space = ' ';

// 4. Boolean type - true/false, cannot be both

// bool isRightAnswer = true;
// bool isWrongAnswer = false;

// 5. String (also called text) it is a combination of 1 or more characters

string message = "I am learning C# 2026";


